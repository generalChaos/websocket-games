/**
 * Game configuration re-exported from local types
 * This file is maintained for backward compatibility
 * All configuration is now centralized in the local types package
 */

export { GameConfig, GameConfigType, GameConfigHelpers } from '../shared/types';
